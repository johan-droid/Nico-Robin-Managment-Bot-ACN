[package]
name = "nico_robin_bot"
version = "0.2.0"
edition = "2021"
authors = ["johan-droid <sahooashutosh2022@gmail.com>"]
default-run = "nico_robin_bot"
autobins = false

[lib]
crate-type = ["rlib"]
path = "src/lib.rs"

[[bin]]
name = "nico_robin_bot"
path = "src/main.rs"

[[bin]]
name = "migrate"
path = "src/bin/migrate.rs"

[[bin]]
name = "diagnose"
path = "src/bin/diagnose.rs"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
envy = "0.4"
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
async-trait = "0.1"
futures-util = "0.3"
url = "2.5"
hex = "0.4"
aes-gcm = "0.10"
base64 = "0.22"
sha2 = "0.10"
hmac = "0.12"
tokio = { version = "1.38", default-features = false, features = ["macros", "sync", "rt", "rt-multi-thread", "io-util", "time", "signal"] }
dotenvy = "0.15"
tokio-postgres = { version = "0.7", default-features = false, features = ["with-chrono-0_4", "with-serde_json-1", "runtime"] }
deadpool-postgres = { version = "0.14", default-features = false, features = ["serde", "rt_tokio_1"] }
uuid = { version = "1.24.0", features = ["v4"] }
axum = { version = "0.7", default-features = false, features = ["json", "tokio", "http1", "macros"] }
hyper = { version = "1.0", features = ["server", "http1"] }
tower = "0.4"
tower-http = { version = "0.5", features = ["cors", "trace"] }
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls", "multipart"] }
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
tokio-postgres-rustls = "0.13"
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
webpki-roots = "0.26"
sentry = { version = "0.48.5", default-features = false, features = ["backtrace", "contexts", "panic", "reqwest", "rustls"] }
image = { version = "0.25", default-features = false, features = ["png", "webp"] }
ab_glyph = "0.2"
subtle = "2.5"
rand = "0.10.2"
