1. Audit src/bot/bot/handlers_list.py: Replace all placeholder callback references (like welcome_plugin.help_cmd) with their real command handlers.
   - For `/management` -> `feature_management_plugin.management_help`
   - For `/features` -> `feature_management_plugin.features`
   - For `/ban` -> `admin_plugin.ban`
   - For `/unban` -> `admin_plugin.unban`
   - For `/kick` -> `admin_plugin.kick`
   - For `/mute` -> `admin_plugin.mute`
   - For `/warn` -> `admin_plugin.warn`
   - Need to import `feature_management_plugin` and `admin_plugin` in `handlers_list.py`

2. Make command registration deterministic:
   - Centralize command registration in `handlers_list.py`'s `register_command_handlers` function.
   - Wait, `dispatcher.py` loads plugins, which register handlers. `handlers_list.py` registers the ones in `COMMAND_BINDINGS`.
   - Update `handlers_list.py` to register all commands listed in `COMMAND_BINDINGS` by skipping registration in plugin `register()` methods for those. Actually, it says "Either A) centralized registry ... or B) plugin register() ... Prefer A". I will modify the plugin `register` functions to NOT register `CommandHandler`s for the ones in `COMMAND_BINDINGS` to avoid duplication. Wait, the instructions say "plugin register() functions only register passive MessageHandlers/CallbackQueryHandlers".
   - Wait, `handlers_list.py` already registers them using `COMMAND_BINDINGS`. I will remove `CommandHandler` registrations from the `register()` functions of plugins, keeping only `MessageHandler` and `CallbackQueryHandler` etc.

3. Fix privilege implementation:
   - `admin_only`: Update to reject anonymous admin (already does in `src/bot/utils/decorators.py`, need to ensure it only applies to sensitive commands or does correctly reject). Wait, `admin_only` currently checks `is_anonymous_admin_message(message)`. But it rejects it globally. Need to ensure it meets: "Telegram admin/owner only, rejects anonymous admin for sensitive commands." Wait, `admin_only` is for sensitive commands.
   - `captain_or_commander_only`: ACN captain/commander only unless explicitly documented otherwise. Update `require_captain_commander` in `decorators.py` to check `ACNService.is_captain()` or `ACNService.is_commander()`. Wait, it currently allows `is_admin_or_owner(..., chat, context)` if chat is provided. Need to fix it. "ACN captain/commander only unless explicitly documented otherwise." If it shouldn't allow Telegram admins by default, I should remove `is_admin_or_owner` check and strictly check `is_captain` or `is_commander`, unless it falls back to something else.
   - `owner_only/sudo_only`: `owner_only` in `decorators.py` already checks `is_sudo`, then group owner, then `is_telegram_owner`.
   - `bot_rights_required`: "checks bot capability separately from user authority". Wait, `bot_rights_required` decorator currently does `await bot_has_admin_rights(context, chat.id, *rights)`. This seems fine, but we should make sure it works correctly.
   - The task states "Fix ACNService.is_commander(): It must return true for env COMMANDER_IDS and DB whitelist role == commander."
     Currently `is_commander` only checks `user_id in ACNService.COMMANDER_IDS`. It should also query the DB whitelist or `ACNService.get_user_role`.

4. Fix ACNService.is_commander():
   - Update `is_commander` to check `user_id in ACNService.COMMANDER_IDS` or `await ACNService.get_user_role(user_id) == "commander"`.

5. Ensure feature_gate.py uses the corrected command registry and all feature_gate names exist in FeatureService.AVAILABLE_FEATURES.
   - `handlers_list.py` `CommandBinding`s specify `feature_gate`. Check that all of these exist in `FeatureService.AVAILABLE_FEATURES`.

6. Add tests:
   - every command in COMMAND_BINDINGS maps to the expected callback, not help_cmd accidentally.
   - no duplicate command registrations.
   - DB-added commander passes captain_or_commander checks.
   - Telegram admin does not pass captain_or_commander unless policy explicitly says so.
   - disabled feature blocks command before handler execution.
